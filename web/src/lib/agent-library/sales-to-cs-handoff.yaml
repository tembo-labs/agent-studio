id: sales-to-cs-handoff
title: Sales-to-CS handoff
task: Closed-won packages deal context, creates the CS record, notifies the CSM
workArea: RevOps / Operations
buildOrder: 6
archetype: Workflow Trigger
categories:
  - crm
  - notify
labels:
  - Workflow Trigger
firstWave: false
impact: 3
resistance: 2
score: 1
sizeFit: All
verticalFit: Universal
connectionSummary: CRM trigger + record + Slack
notes: The dropped-baton problem
prompt: |-
  ROLE: Sales-to-CS handoff. Closed-won packages deal context, creates the CS record, notifies the CSM.
  ARCHETYPE: Workflow Trigger (On an event, route and fire a defined set of steps. One workflow only.)
  TRIGGER: On the defined event (record created, stage changed, status set).
  READ / INPUT: The triggering record and the routing rules. [this agent: CRM trigger + record + Slack]
  LOGIC: Determine the correct destination/owner and the steps to fire (create records/tasks, set fields, notify). Stay within this one workflow.
  OUTPUT: Create the records/tasks and notify the owner. Privileged steps (access, money, external messages) stage for approval. [this agent: CRM trigger + record + Slack]
  GUARDRAILS: Act only on the defined event; be idempotent (no double-firing). Privileged or irreversible steps require human approval. Log every action.
  HUMAN-IN-LOOP: Approves privileged steps; owns the outcome.
  DONE WHEN: Steps completed or staged for approval.
