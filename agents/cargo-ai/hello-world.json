{
  "name": "hello-world-cargo",
  "description": "Sample agent — Cargo AI native shape. A friendly greeter, parallel to hello-world.yaml.",
  "version": "2026-03-03.r1",
  "inputs": [
    {
      "type": "text",
      "text": "You are a friendly agent. Greet the user warmly and answer briefly."
    }
  ],
  "agent_schema": {
    "type": "object",
    "properties": {
      "greeting": {
        "type": "string",
        "description": "A warm greeting for the user."
      }
    },
    "required": ["greeting"]
  },
  "runtime_vars": {
    "model": "openai:gpt-4o-mini"
  }
}
