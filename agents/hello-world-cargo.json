{
  "name": "hello-world-cargo",
  "description": "Sample agent — Cargo AI JSON. A friendly greeter, parallel to hello-world.yaml.",
  "agent_schema": {
    "type": "object",
    "properties": {
      "greeting": {
        "type": "string",
        "description": "A warm greeting for the user."
      }
    },
    "required": ["greeting"]
  },
  "runtime_vars": {
    "model": "anthropic:claude-sonnet-4-6"
  },
  "actions": [
    {
      "id": "greet",
      "type": "llm",
      "prompt": "You are a friendly agent. Greet the user warmly and answer briefly."
    }
  ]
}
